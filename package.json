{
  "name": "chadaalyasmin-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "vercel-build": "cd next-app && npm install && npm run build",
    "build": "cd next-app && npm install && npm run build",
    "dev": "npm --prefix next-app run dev",
    "start": "npm --prefix next-app run start"
  },
  "dependencies": {
    "@openrouter/agent": "^0.7.2"
  }
}
